# Project Instructions

本项目使用 `.codex/` 目录维护 ClickHouseDB / SQL / 数据分析相关的辅助上下文。
每次处理本项目相关的 ClickHouse / SQL / 数据分析任务前，都应将.codex/memory.md 中与当前任务相关规则加载进上下文。

---

## 1. ClickHouseDB SQL 工作规则

当需要编写或修改本项目相关的 ClickHouseDB SQL 时，必须先执行以下步骤：

1. 识别本次 SQL 涉及的表名。
2. 查询 `.codex/` 目录下是否存在与表同名的 `.xlsx` 文件。
3. 如果存在对应的 `.xlsx` 文件，必须读取其内容，并将其中与本次任务相关的表定义、字段含义、字段类型、业务说明、注意事项等信息纳入当前上下文。
如果未找到对应 `.xlsx` 文件，则不需要加载进上下文
4. 如果本次 SQL 涉及多张表，则需要分别查询并读取每张表对应的 `.xlsx` 文件。
5. 在正式编写或修改 SQL 前，简要说明已读取到的表结构信息来源。

示例：

- 表名：`orders`
- 优先查询：`.codex/orders.xlsx`


---

## 2. 表结构与字段定义优先级

处理 ClickHouseDB SQL 时，必须优先遵守 `.codex/` 下与表同名的 `.xlsx` 文件中已经确认的信息。

如果 `.xlsx` 文件中的信息与用户当前描述冲突，不能自行判断或静默覆盖，必须明确指出冲突并向用户确认。

---

## 3. 未知信息处理规则

如果表、字段、指标或业务口径未知，不要假装确定。

不要自行编造表名、字段名、指标口径或业务规则。需要时必须向用户询问。

---

## 4. `.codex/memory.md` 使用规则

`.codex/memory.md` 用于记录长期可复用、已确认的 ClickHouseDB / SQL / 数据分析经验。

只有当用户明确指出需要记录信息时，才更新 `.codex/memory.md`

不要为了记录而记录。如果你不清楚是否需要添加，则不添加。

以下内容不应写入 `.codex/memory.md`：

- 一次性查询条件；
- 临时日期范围；
- 未确认的猜测；
- 普通过程性说明；
- 没有复用价值的分析结果。

---

## 5. 安全规则

不要在 `.codex/memory.md` 或其他项目记忆文件中保存以下内容：

- 密码
- token
- API key
- 密钥
- 连接凭据
- 个人隐私
- 敏感账号
- 内部敏感访问地址
- 任何不应进入版本管理的信息

如果用户提供了敏感信息，只能在当前任务中临时使用，不得写入 `.codex/memory.md`。

---

## 6. 回答格式要求

在编写或修改 ClickHouseDB SQL 前，应简要说明本次读取到的上下文，如果表结构信息明确且无冲突，说明应简洁，不需要重复完整表结构；只列出本次 SQL 直接相关的字段和口径。例如：

```text
我已检查 `.codex/` 目录，并读取了以下表结构文件：

- `.codex/orders.xlsx`
- `.codex/users.xlsx`

本次相关字段信息包括：...

```