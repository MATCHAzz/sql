# 短信电话召回会员分析对话总结

## 1. 分析目标

本次分析目标是分析 **2026 年 2-4 月短信电话付费召回带来的会员付费人群特征和站内行为路径**，并据此判断后续如何优化召回策略、站内承接和人群投放。

重点回答：

- 短信电话付费召回后，真正产生会员付费的用户是谁？他们在 `seven_class`、平台、沉默天数、召回渠道等维度上有什么特征？
- 这些会员付费用户在站内是如何走到付费的？典型路径是否集中在职位详情、聊天/沟通受限、会员权益页、下单页等关键节点？
- 不同人群、Android/iOS、召回渠道的短期付费转化和累计净收入贡献有什么差异？
- 后续应如何优化：哪些人群加量，哪些人群降频，哪些路径需要强化权益承接或调整召回话术？

报告主线建议：

> 先用会员付费底表识别短信电话召回后的付费人群特征和收入贡献，再补站内行为路径，判断付费用户从召回到会员购买的关键触点，最终输出分人群、分平台、分渠道的优化建议。

## 2. 核心口径

时间范围：

- 召回时间：`2026-02-01` 至 `2026-04-30`
- 当前底表收入统计截止：`pay_end_date = end_date`，即只统计到 `2026-04-30`

召回用户：

- 来自 `dwm.dw_growth_user_sms_recall_di`
- 与 `dwm.dw_growth_user_active_di` 按 `uid + d` 关联
- 限制：`silent_days >= 30`
- 限制：召回当天 `is_member = 0`

收入口径：

- 收入表：`dwd.dw_member_deal_all_da`
- 剔除 `mem_id IN (6,10)`
- 剔除 iOS 外币异常：

```sql
NOT (
    d.is_ios = 1
    AND d.currency NOT IN ('USD', 'CNY', '')
    AND toDateTime(d.income_ts) <= '2024-06-01'
)
```

黑产口径：

- 使用 `dim.dim_user.is_blocked`
- 底表收入指标中通过 `blocked_flag = 0` 排除黑产用户

## 3. 关键口径讨论结论

### 3.1 是否需要排除召回次日成为会员用户

不应该排除。

原因：

- 召回当天非会员、召回后次日成为会员，正是本次召回希望捕捉的转化。
- 原 SQL 里 `dim_member` 的逻辑容易被误解，不应简单理解为“次日仍非会员”过滤。
- 当前底表保留召回后转化用户，更适合分析召回效果。

### 3.2 为什么 7 日净收入会为负

`net_income_7d` 为负是合理的，因为净收入包含退款/冲正。

验证结果：

- `net_income_7d < 0` 的用户数：`19`
- 其中召回前有会员正向支付记录的用户数：`19`

结论：

> 7 日净收入为负主要是历史会员订单的退款/冲正落入召回后 7 日窗口，而不是召回后先退款再购买。

因此：

- 7 日窗口适合看短期转化。
- 7 日净收入不适合做主收入结论。
- 7 日短期收入建议看正向支付收入。
- 收入贡献建议看召回后累计净收入。

### 3.3 为什么仍然保留 7 日窗口

7 日窗口的意义是做归因约束：

- 召回后短期付费更可能与召回动作相关。
- 拉长时间后的收入可能混入自然回流、其他运营动作、站内行为、自动续费等影响。

最终口径：

- `7日窗口`：看短期转化效率和短期正向支付。
- `召回后累计`：看该批召回用户的收入贡献。

## 4. 当前底表状态

当前底表文件：

`/Users/matchalee/Desktop/sql/会员底表.sql`

底表粒度：

- 一行一个召回用户
- 以用户在 2-4 月首次召回日期为 `recall_date`

当前底表字段：

```text
uid
recall_date
recall_month
platform_type
seven_class
silent_days
recall_tags
is_member_pay_7d
first_pay_date_7d
member_pay_income_7d_exclude_blocked
member_refund_income_7d_exclude_blocked
member_net_income_7d_exclude_blocked
member_pay_income_after_recall_exclude_blocked
member_refund_income_after_recall_exclude_blocked
member_net_income_after_recall_exclude_blocked
```

字段解释：

- `is_member_pay_7d`：召回后 7 日内是否发生会员正向支付。
- `first_pay_date_7d`：召回后 7 日内首次会员正向支付日期；无则为 `1970-01-01`。
- `member_pay_income_7d_exclude_blocked`：召回后 7 日内正向支付收入，剔除黑产。
- `member_refund_income_7d_exclude_blocked`：召回后 7 日内退款/冲正收入，剔除黑产。
- `member_net_income_7d_exclude_blocked`：召回后 7 日内净收入，剔除黑产。
- `member_pay_income_after_recall_exclude_blocked`：召回后至统计截止日累计正向支付收入，剔除黑产。
- `member_refund_income_after_recall_exclude_blocked`：召回后至统计截止日累计退款/冲正收入，剔除黑产。
- `member_net_income_after_recall_exclude_blocked`：召回后至统计截止日累计净收入，剔除黑产。

`seven_class` 已处理空值：

```sql
ifNull(nullIf(trimBoth(toString(seven_class)), ''), '未知')
```

## 5. 已观察到的人群收入结构

之前 2-4 月排黑产会员净收入表中，收入主力为：

- `新经济头部`
- `新经济腰尾`
- `传统主流一线`

按修正后的 2-4 月表，三类合计贡献约 84% 的会员净收入。

初步判断：

- `新经济头部 / 新经济腰尾`：规模大且收入效率较好，是核心优化人群。
- `传统主流一线`：规模最大，但单位召回用户收入相对弱，更偏规模盘。
- `其他主流行业`：用户规模小但收入效率高，适合进一步看是否能扩量。
- `学生 / 传统行业其他`：相对低效，适合看是否降频或改低成本触达。

## 6. 汇总分析 SQL 模板

下面 SQL 均需要将：

```sql
FROM (
    -- 这里放会员底表 SQL
) t
```

替换为当前完整底表 SQL。

### 6.1 底表校验

```sql
SELECT
    uniqExact(uid) AS recall_user_cnt,
    count() AS row_cnt,
    uniqExactIf(uid, is_member_pay_7d = 1) AS pay_user_cnt_7d,
    sum(member_pay_income_7d_exclude_blocked) AS pay_income_7d,
    sum(member_refund_income_7d_exclude_blocked) AS refund_income_7d,
    sum(member_net_income_7d_exclude_blocked) AS net_income_7d,
    sum(member_net_income_after_recall_exclude_blocked) AS net_income_after_recall
FROM (
    -- 这里放会员底表 SQL
) t;
```

### 6.2 人群贡献表

```sql
SELECT
    seven_class,
    recall_user_cnt,
    pay_user_cnt_7d,
    pay_rate_7d,
    pay_income_7d,
    refund_income_7d,
    net_income_7d,
    net_income_after_recall,
    pay_income_7d_per_recall_user,
    pay_arppu_7d,
    net_income_after_recall_per_user
FROM (
    SELECT
        ifNull(nullIf(seven_class, ''), '未知') AS seven_class,
        uniqExact(uid) AS recall_user_cnt,
        uniqExactIf(uid, is_member_pay_7d = 1) AS pay_user_cnt_7d,
        sum(member_pay_income_7d_exclude_blocked) AS pay_income_7d,
        sum(member_refund_income_7d_exclude_blocked) AS refund_income_7d,
        sum(member_net_income_7d_exclude_blocked) AS net_income_7d,
        sum(member_net_income_after_recall_exclude_blocked) AS net_income_after_recall,
        pay_user_cnt_7d / recall_user_cnt AS pay_rate_7d,
        pay_income_7d / recall_user_cnt AS pay_income_7d_per_recall_user,
        if(pay_user_cnt_7d = 0, 0, pay_income_7d / pay_user_cnt_7d) AS pay_arppu_7d,
        net_income_after_recall / recall_user_cnt AS net_income_after_recall_per_user
    FROM (
        -- 这里放会员底表 SQL
    ) t
    GROUP BY seven_class
)
ORDER BY net_income_after_recall DESC;
```

### 6.3 Android / iOS × 人群

```sql
SELECT
    platform_type,
    seven_class,
    recall_user_cnt,
    pay_user_cnt_7d,
    pay_rate_7d,
    pay_income_7d,
    net_income_after_recall,
    pay_income_7d_per_recall_user,
    net_income_after_recall_per_user
FROM (
    SELECT
        platform_type,
        ifNull(nullIf(seven_class, ''), '未知') AS seven_class,
        uniqExact(uid) AS recall_user_cnt,
        uniqExactIf(uid, is_member_pay_7d = 1) AS pay_user_cnt_7d,
        sum(member_pay_income_7d_exclude_blocked) AS pay_income_7d,
        sum(member_net_income_after_recall_exclude_blocked) AS net_income_after_recall,
        pay_user_cnt_7d / recall_user_cnt AS pay_rate_7d,
        pay_income_7d / recall_user_cnt AS pay_income_7d_per_recall_user,
        net_income_after_recall / recall_user_cnt AS net_income_after_recall_per_user
    FROM (
        -- 这里放会员底表 SQL
    ) t
    GROUP BY
        platform_type,
        seven_class
)
ORDER BY net_income_after_recall DESC;
```

### 6.4 召回渠道贡献

```sql
SELECT
    recall_channel,
    recall_user_cnt,
    pay_user_cnt_7d,
    pay_rate_7d,
    pay_income_7d,
    net_income_after_recall,
    pay_income_7d_per_recall_user
FROM (
    SELECT
        multiIf(
            toString(recall_tags) LIKE '%phone%', 'phone',
            toString(recall_tags) LIKE '%sms%', 'sms',
            toString(recall_tags) LIKE '%wechat%', 'wechat',
            toString(recall_tags) LIKE '%push%', 'push',
            'other'
        ) AS recall_channel,
        uniqExact(uid) AS recall_user_cnt,
        uniqExactIf(uid, is_member_pay_7d = 1) AS pay_user_cnt_7d,
        sum(member_pay_income_7d_exclude_blocked) AS pay_income_7d,
        sum(member_net_income_after_recall_exclude_blocked) AS net_income_after_recall,
        pay_user_cnt_7d / recall_user_cnt AS pay_rate_7d,
        pay_income_7d / recall_user_cnt AS pay_income_7d_per_recall_user
    FROM (
        -- 这里放会员底表 SQL
    ) t
    GROUP BY recall_channel
)
ORDER BY net_income_after_recall DESC;
```

### 6.5 月份 × 平台趋势

```sql
SELECT
    recall_month,
    platform_type,
    uniqExact(uid) AS recall_user_cnt,
    uniqExactIf(uid, is_member_pay_7d = 1) AS pay_user_cnt_7d,
    sum(member_pay_income_7d_exclude_blocked) AS pay_income_7d,
    sum(member_net_income_after_recall_exclude_blocked) AS net_income_after_recall,
    pay_user_cnt_7d / recall_user_cnt AS pay_rate_7d,
    pay_income_7d / recall_user_cnt AS pay_income_7d_per_recall_user
FROM (
    -- 这里放会员底表 SQL
) t
GROUP BY
    recall_month,
    platform_type
ORDER BY
    recall_month,
    platform_type;
```

## 7. 报告建议结构

建议报告结构：

1. 核心结论
2. 数据口径说明
3. 大盘表现：召回用户数、7 日付费率、7 日正向支付收入、累计净收入
4. 人群贡献：按 `seven_class`
5. 平台差异：Android / iOS
6. 召回渠道差异：phone / sms / wechat / push / other
7. 行为路径分析：后续补充站内行为漏斗
8. 优化建议

核心表优先级：

1. 人群贡献表
2. Android / iOS × 人群表
3. 召回渠道贡献表
4. 月份 × 平台趋势表

## 8. 结论表达建议

可以使用如下口径：

> 7 日窗口主要用于衡量召回后的短期正向付费转化；由于 7 日净收入可能受到历史订单退款/冲正落窗影响，本文不以 7 日净收入作为主要收入判断。收入贡献判断以召回后累计净收入为主，7 日正向支付收入作为短期转化收入辅助指标。

## 9. 下一步待做

建议下一步依次完成：

1. 跑底表校验，确认 `row_cnt = recall_user_cnt`。
2. 跑人群贡献表，确认收入主力和效率人群。
3. 跑 Android / iOS × 人群表，看平台差异。
4. 跑召回渠道贡献表，确认 phone/sms/wechat/push 的效率。
5. 补站内行为路径底表：
   - 触达后回访
   - 职位详情页
   - 聊天页/沟通受限
   - 会员权益页
   - 下单页
   - 支付成功
6. 对付费用户和未付费用户做行为差异对比。

## 10. 新窗口提示词

```text
我在做“2026 年 2-4 月短信电话召回用户的会员付费分析”。当前已有用户粒度底表 SQL，文件路径是 /Users/matchalee/Desktop/sql/会员底表.sql。

分析目标：
1. 看短信电话召回后的会员付费人群特征。
2. 分 Android / iOS 看平台差异。
3. 看 seven_class 人群收入贡献和转化效率。
4. 后续补站内行为路径，判断如何优化召回策略。

当前核心口径：
- 召回时间：2026-02-01 至 2026-04-30。
- 召回用户来自 dwm.dw_growth_user_sms_recall_di。
- 与 dwm.dw_growth_user_active_di 按 uid + d 关联。
- 限制 silent_days >= 30 且召回当天 is_member = 0。
- 收入来自 dwd.dw_member_deal_all_da。
- 排除 mem_id IN (6,10)，排除 iOS 外币异常。
- 黑产用 dim.dim_user.is_blocked 剔除。
- 底表一行一个用户，以 2-4 月首次召回日期为 recall_date。

关键字段：
- is_member_pay_7d：召回后 7 日是否正向会员付费。
- member_pay_income_7d_exclude_blocked：7 日正向支付收入。
- member_refund_income_7d_exclude_blocked：7 日退款/冲正收入。
- member_net_income_7d_exclude_blocked：7 日净收入，仅辅助观察。
- member_net_income_after_recall_exclude_blocked：召回后累计净收入，作为收入贡献主指标。

重要结论：
- 7 日净收入可能为负，原因是历史会员订单退款/冲正落入召回后 7 日窗口。
- 已验证 net_income_7d < 0 的 19 个用户里，19 个都有召回前会员正向支付记录。
- 因此 7 日窗口主要用于衡量短期转化，不用 7 日净收入做主收入结论。
- 收入贡献以召回后累计净收入为主，短期效果以 7 日付费率和 7 日正向支付收入为主。

请先读取 /Users/matchalee/Desktop/sql/会员底表.sql，帮我继续做：
1. 底表校验。
2. 人群贡献分析。
3. Android/iOS × 人群分析。
4. 召回渠道贡献分析。
5. 后续站内行为路径分析方案和 SQL。
```
